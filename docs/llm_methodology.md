# LLM Methodology

LLM outputs are annotations, not source-derived facts. Every model-derived field should
record provider, model, prompt name/version, inference parameters, timestamp, status, and
optional retained reasoning.

