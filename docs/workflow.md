# Workflow

Initial migration checkpoint covers local, testable components only: data/provenance
models, normalization, BibTeX parsing/writing, and deduplication.

Network source adapters, paper download, and live model calls are intentionally deferred.

