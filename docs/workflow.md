# Workflow

Initial migration checkpoint covered local, testable components only: data/provenance
models, normalization, BibTeX parsing/writing, and deduplication.

Phase 2 adds mocked literature source adapters and OA PDF acquisition. Live source
queries, live PDF downloads, and live model calls remain intentionally disabled.
