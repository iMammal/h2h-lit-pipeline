# H2H Literature Pipeline

Clean-room package for the literature-research workflow reconstructed from the
`PyCharmMiscProject` archaeology workspace.

This repository intentionally starts without historical Git history, generated corpora,
notebook outputs, PDFs, or credentials. Reusable implementation belongs under `src/`;
notebooks are reserved for small, deliberate demonstrations that call the package.

